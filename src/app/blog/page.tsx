import type { Metadata } from "next";
import Link from "next/link";
import { SITE, BLOG_POSTS } from "@/data/config";
export const metadata: Metadata = { title: "Blog", description: `Writing on AI, data, AWS, marketing, and research by ${SITE.name}.` };
function formatDate(iso: string) { return new Date(iso).toLocaleDateString("en-US", { year: "numeric", month: "long", day: "numeric" }); }
export default function BlogPage() { return <><section className="soft-grad"><div className="mx-auto max-w-5xl px-6 pb-16 pt-20"><p className="accent-text mb-3 text-sm font-semibold uppercase tracking-widest">Blog</p><h1 className="mb-4 text-4xl font-bold tracking-tight text-[#0F172A] md:text-5xl">Writing</h1><p className="max-w-xl text-lg text-slate-600">Thoughts on AI, data, AWS, marketing, and research.</p></div></section><section className="border-t border-slate-100 py-20"><div className="mx-auto max-w-3xl px-6">{BLOG_POSTS.length === 0 ? <div className="rounded-3xl border border-blue-100 bg-blue-50/60 p-8 md:p-10"><p className="accent-text mb-3 text-sm font-semibold uppercase tracking-widest">Coming soon</p><h2 className="text-2xl font-bold tracking-tight text-[#0F172A]">Posts coming soon</h2><p className="mt-3 max-w-xl leading-relaxed text-slate-600">Writing on AI, data, AWS, marketing, and research. Posts coming soon.</p></div> : <ul className="divide-y divide-slate-100">{BLOG_POSTS.map((post) => <li key={post.slug} className="py-8 first:pt-0"><Link href={`/blog/${post.slug}`} className="group block"><div className="mb-2 flex flex-wrap gap-3 text-xs text-slate-400"><time dateTime={post.date}>{formatDate(post.date)}</time><span>·</span><span>{post.readingTime}</span>{post.tags.map((tag) => <span key={tag} className="rounded-full bg-slate-100 px-2.5 py-0.5">{tag}</span>)}</div><h2 className="mb-2 text-xl font-semibold text-[#0F172A] transition group-hover:text-[#2563EB]">{post.title}</h2><p className="text-sm leading-relaxed text-slate-600">{post.excerpt}</p><span className="accent-text mt-3 inline-block text-sm font-medium group-hover:underline">Read more →</span></Link></li>)}</ul>}</div></section></>; }
